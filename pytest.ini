# pytest.ini
[pytest]
markers =
    deep: реальные, долгие интеграционные тесты с моделями
addopts = -m "not deep"

filterwarnings =
    ignore:FP16 is not supported on CPU; using FP32 instead:UserWarning:whisper\.
    ignore:Module 'speechbrain\.pretrained' was deprecated.*:UserWarning